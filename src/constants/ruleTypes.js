const AND = 'AND'
const OR = 'OR'
const XOR = 'XOR'
const NAND = 'NAND'
const NOR = 'NOR'
const NXOR = 'NXOR'
const EXACTLY_ONE = 'EXACTLY_ONE'
const TERMINAL = 'TERMINAL'

module.exports = {
  AND,
  OR,
  XOR,
  NAND,
  NOR,
  NXOR,
  EXACTLY_ONE,
  TERMINAL,
}